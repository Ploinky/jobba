name: Build Release

on: push

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@latest
      - uses: microsoft/setup-msbuild@latest
      - run: msbuild jobba.vcxproj -t:rebuild -verbosity:diag -property:Configuration=Release
